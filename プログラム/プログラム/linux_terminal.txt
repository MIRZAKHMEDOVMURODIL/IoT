admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Code$ ./mount.sh
bash: ./mount.sh: 許可がありません
admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Code$ ./mount.sh
bash: ./mount.sh: 許可がありません
admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Code$ ./mount.sh
bash: ./mount.sh: 許可がありません
admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Code$ ls
mount.sh
admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Code$ cd ..
admin@InterfaceLinuxSystem:~/デスクトップ/IoT$ ls
Code  Swap  Temp
admin@InterfaceLinuxSystem:~/デスクトップ/IoT$ cd Temp/
admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Temp$ pwd
/home/admin/デスクトップ/IoT/Temp
admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Temp$ cd..
bash: cd..: コマンドが見つかりません
admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Temp$ cd ..
admin@InterfaceLinuxSystem:~/デスクトップ/IoT$ cd Code/
admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Code$ ls
mount.sh
admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Code$ ./mount.sh
bash: ./mount.sh: 許可がありません
admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Code$ which mount
/bin/mount
admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Code$ ls -al /bin/mount
-rwsr-xr-x 1 root root 44304  3月  8  2018 /bin/mount
admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Code$ su -
パスワード:
su: 認証失敗
admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Code$ su -
パスワード:
su: 認証失敗
admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Code$ su
パスワード:
su: 認証失敗
admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Code$ su 
パスワード:
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# ./mount.sh
bash: ./mount.sh: 許可がありません
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# ls -al
合計 12
drwxr-xr-x 2 admin admin 4096  5月 15 14:25 .
drwxr-xr-x 5 admin admin 4096  5月 15 15:06 ..
-rw-r--r-- 1 admin admin  428  5月 15 16:15 mount.sh
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# chmod 744 mount.sh 
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# ls -al
合計 12
drwxr-xr-x 2 admin admin 4096  5月 15 14:25 .
drwxr-xr-x 5 admin admin 4096  5月 15 15:06 ..
-rwxr--r-- 1 admin admin  428  5月 15 16:15 mount.sh
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# chmod 755 mount.sh 
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# ls -al
合計 12
drwxr-xr-x 2 admin admin 4096  5月 15 14:25 .
drwxr-xr-x 5 admin admin 4096  5月 15 15:06 ..
-rwxr-xr-x 1 admin admin  428  5月 15 16:15 mount.sh
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# ls /usr
bin  games  include  lib  local  sbin  share  src
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# ls -al /usr
合計 152
drwxr-xr-x  10 root root   4096  3月 29  2018 .
drwxr-xr-x  25 root root   4096  5月  1 16:46 ..
drwxr-xr-x   2 root root  69632  5月 10 12:53 bin
drwxr-xr-x   2 root root   4096  7月  6  2017 games
drwxr-xr-x 147 root root  20480  5月  9 14:57 include
drwxr-xr-x 167 root root  20480  5月  9 14:57 lib
drwxrwsr-x  10 root staff  4096  1月  1  1970 local
drwxr-xr-x   2 root root  12288  5月  9 14:57 sbin
drwxr-xr-x 370 root root  12288  5月 10 12:53 share
drwxr-xr-x   7 root root   4096 12月  6 15:26 src
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# ls -al
合計 12
drwxr-xr-x 2 admin admin 4096  5月 15 14:25 .
drwxr-xr-x 5 admin admin 4096  5月 15 15:06 ..
-rwxr-xr-x 1 admin admin  428  5月 15 16:15 mount.sh
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# ./mount.sh

Usage:
 mount [-lhV]
 mount -a [options]
 mount [options] [--source] <source> | [--target] <directory>
 mount [options] <source> <directory>
 mount <operation> <mountpoint> [<target>]

Mount a filesystem.

Options:
 -a, --all               mount all filesystems mentioned in fstab
 -c, --no-canonicalize   don't canonicalize paths
 -f, --fake              dry run; skip the mount(2) syscall
 -F, --fork              fork off for each device (use with -a)
 -T, --fstab <path>      alternative file to /etc/fstab
 -i, --internal-only     don't call the mount.<type> helpers
 -l, --show-labels       show also filesystem labels
 -n, --no-mtab           don't write to /etc/mtab
 -o, --options <list>    comma-separated list of mount options
 -O, --test-opts <list>  limit the set of filesystems (use with -a)
 -r, --read-only         mount the filesystem read-only (same as -o ro)
 -t, --types <list>      limit the set of filesystem types
     --source <src>      explicitly specifies source (path, label, uuid)
     --target <target>   explicitly specifies mountpoint
 -v, --verbose           say what is being done
 -w, --rw, --read-write  mount the filesystem read-write (default)

 -h, --help     display this help and exit
 -V, --version  output version information and exit

Source:
 -L, --label <label>     synonym for LABEL=<label>
 -U, --uuid <uuid>       synonym for UUID=<uuid>
 LABEL=<label>           specifies device by filesystem label
 UUID=<uuid>             specifies device by filesystem UUID
 PARTLABEL=<label>       specifies device by partition label
 PARTUUID=<uuid>         specifies device by partition UUID
 <device>                specifies device by path
 <directory>             mountpoint for bind mounts (see --bind/rbind)
 <file>                  regular file for loopdev setup

Operations:
 -B, --bind              mount a subtree somewhere else (same as -o bind)
 -M, --move              move a subtree to some other place
 -R, --rbind             mount a subtree and all submounts somewhere else
 --make-shared           mark a subtree as shared
 --make-slave            mark a subtree as slave
 --make-private          mark a subtree as private
 --make-unbindable       mark a subtree as unbindable
 --make-rshared          recursively mark a whole subtree as shared
 --make-rslave           recursively mark a whole subtree as slave
 --make-rprivate         recursively mark a whole subtree as private
 --make-runbindable      recursively mark a whole subtree as unbindable

For more details see mount(8).
Failed to mount
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# chmod 775 mount.sh 
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# exit
exit
admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Code$ ./mount.sh
mount: only root can use "--options" option
Failed to mount
admin@InterfaceLinuxSystem:~/デスクトップ/IoT/Code$ su -
パスワード:
root@InterfaceLinuxSystem:~# cd /home/admin/
.cache/         .dbus/          .gnupg/         .mozc/          .pcsc10/        .vnc/           interface/      テンプレート/   ドキュメント/   音楽/           公開/           
.config/        .gconf/         .local/         .nano/          .presage/       .vscode-server/ ダウンロード/   デスクトップ/   ビデオ/         画像/           
root@InterfaceLinuxSystem:~# cd /home/admin/デスクトップ/IoT/
Code/ Swap/ Temp/ 
root@InterfaceLinuxSystem:~# cd /home/admin/デスクトップ/IoT/Code/
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# ./mount.sh 

Usage:
 mount [-lhV]
 mount -a [options]
 mount [options] [--source] <source> | [--target] <directory>
 mount [options] <source> <directory>
 mount <operation> <mountpoint> [<target>]

Mount a filesystem.

Options:
 -a, --all               mount all filesystems mentioned in fstab
 -c, --no-canonicalize   don't canonicalize paths
 -f, --fake              dry run; skip the mount(2) syscall
 -F, --fork              fork off for each device (use with -a)
 -T, --fstab <path>      alternative file to /etc/fstab
 -i, --internal-only     don't call the mount.<type> helpers
 -l, --show-labels       show also filesystem labels
 -n, --no-mtab           don't write to /etc/mtab
 -o, --options <list>    comma-separated list of mount options
 -O, --test-opts <list>  limit the set of filesystems (use with -a)
 -r, --read-only         mount the filesystem read-only (same as -o ro)
 -t, --types <list>      limit the set of filesystem types
     --source <src>      explicitly specifies source (path, label, uuid)
     --target <target>   explicitly specifies mountpoint
 -v, --verbose           say what is being done
 -w, --rw, --read-write  mount the filesystem read-write (default)

 -h, --help     display this help and exit
 -V, --version  output version information and exit

Source:
 -L, --label <label>     synonym for LABEL=<label>
 -U, --uuid <uuid>       synonym for UUID=<uuid>
 LABEL=<label>           specifies device by filesystem label
 UUID=<uuid>             specifies device by filesystem UUID
 PARTLABEL=<label>       specifies device by partition label
 PARTUUID=<uuid>         specifies device by partition UUID
 <device>                specifies device by path
 <directory>             mountpoint for bind mounts (see --bind/rbind)
 <file>                  regular file for loopdev setup

Operations:
 -B, --bind              mount a subtree somewhere else (same as -o bind)
 -M, --move              move a subtree to some other place
 -R, --rbind             mount a subtree and all submounts somewhere else
 --make-shared           mark a subtree as shared
 --make-slave            mark a subtree as slave
 --make-private          mark a subtree as private
 --make-unbindable       mark a subtree as unbindable
 --make-rshared          recursively mark a whole subtree as shared
 --make-rslave           recursively mark a whole subtree as slave
 --make-rprivate         recursively mark a whole subtree as private
 --make-runbindable      recursively mark a whole subtree as unbindable

For more details see mount(8).
Failed to mount
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# ./mount.sh 

Usage:
 mount [-lhV]
 mount -a [options]
 mount [options] [--source] <source> | [--target] <directory>
 mount [options] <source> <directory>
 mount <operation> <mountpoint> [<target>]

Mount a filesystem.

Options:
 -a, --all               mount all filesystems mentioned in fstab
 -c, --no-canonicalize   don't canonicalize paths
 -f, --fake              dry run; skip the mount(2) syscall
 -F, --fork              fork off for each device (use with -a)
 -T, --fstab <path>      alternative file to /etc/fstab
 -i, --internal-only     don't call the mount.<type> helpers
 -l, --show-labels       show also filesystem labels
 -n, --no-mtab           don't write to /etc/mtab
 -o, --options <list>    comma-separated list of mount options
 -O, --test-opts <list>  limit the set of filesystems (use with -a)
 -r, --read-only         mount the filesystem read-only (same as -o ro)
 -t, --types <list>      limit the set of filesystem types
     --source <src>      explicitly specifies source (path, label, uuid)
     --target <target>   explicitly specifies mountpoint
 -v, --verbose           say what is being done
 -w, --rw, --read-write  mount the filesystem read-write (default)

 -h, --help     display this help and exit
 -V, --version  output version information and exit

Source:
 -L, --label <label>     synonym for LABEL=<label>
 -U, --uuid <uuid>       synonym for UUID=<uuid>
 LABEL=<label>           specifies device by filesystem label
 UUID=<uuid>             specifies device by filesystem UUID
 PARTLABEL=<label>       specifies device by partition label
 PARTUUID=<uuid>         specifies device by partition UUID
 <device>                specifies device by path
 <directory>             mountpoint for bind mounts (see --bind/rbind)
 <file>                  regular file for loopdev setup

Operations:
 -B, --bind              mount a subtree somewhere else (same as -o bind)
 -M, --move              move a subtree to some other place
 -R, --rbind             mount a subtree and all submounts somewhere else
 --make-shared           mark a subtree as shared
 --make-slave            mark a subtree as slave
 --make-private          mark a subtree as private
 --make-unbindable       mark a subtree as unbindable
 --make-rshared          recursively mark a whole subtree as shared
 --make-rslave           recursively mark a whole subtree as slave
 --make-rprivate         recursively mark a whole subtree as private
 --make-runbindable      recursively mark a whole subtree as unbindable

For more details see mount(8).
Failed to mount
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# mount -t cifs -o vers=2.0, domain='AD', user='00220401626', password='Mm3001645$' 'C:/Users/00220401626/Desktop/Server' /home/admin/デスクトップ/IoT/Temp
 
Usage:
 mount [-lhV]
 mount -a [options]
 mount [options] [--source] <source> | [--target] <directory>
 mount [options] <source> <directory>
 mount <operation> <mountpoint> [<target>]

Mount a filesystem.

Options:
 -a, --all               mount all filesystems mentioned in fstab
 -c, --no-canonicalize   don't canonicalize paths
 -f, --fake              dry run; skip the mount(2) syscall
 -F, --fork              fork off for each device (use with -a)
 -T, --fstab <path>      alternative file to /etc/fstab
 -i, --internal-only     don't call the mount.<type> helpers
 -l, --show-labels       show also filesystem labels
 -n, --no-mtab           don't write to /etc/mtab
 -o, --options <list>    comma-separated list of mount options
 -O, --test-opts <list>  limit the set of filesystems (use with -a)
 -r, --read-only         mount the filesystem read-only (same as -o ro)
 -t, --types <list>      limit the set of filesystem types
     --source <src>      explicitly specifies source (path, label, uuid)
     --target <target>   explicitly specifies mountpoint
 -v, --verbose           say what is being done
 -w, --rw, --read-write  mount the filesystem read-write (default)

 -h, --help     display this help and exit
 -V, --version  output version information and exit

Source:
 -L, --label <label>     synonym for LABEL=<label>
 -U, --uuid <uuid>       synonym for UUID=<uuid>
 LABEL=<label>           specifies device by filesystem label
 UUID=<uuid>             specifies device by filesystem UUID
 PARTLABEL=<label>       specifies device by partition label
 PARTUUID=<uuid>         specifies device by partition UUID
 <device>                specifies device by path
 <directory>             mountpoint for bind mounts (see --bind/rbind)
 <file>                  regular file for loopdev setup

Operations:
 -B, --bind              mount a subtree somewhere else (same as -o bind)
 -M, --move              move a subtree to some other place
 -R, --rbind             mount a subtree and all submounts somewhere else
 --make-shared           mark a subtree as shared
 --make-slave            mark a subtree as slave
 --make-private          mark a subtree as private
 --make-unbindable       mark a subtree as unbindable
 --make-rshared          recursively mark a whole subtree as shared
 --make-rslave           recursively mark a whole subtree as slave
 --make-rprivate         recursively mark a whole subtree as private
 --make-runbindable      recursively mark a whole subtree as unbindable

For more details see mount(8).
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# mmount -t cifs -o vers=2.0, domain='AD', user='00220401626', password='Mm3001645$' 'C:/Users/00220401626/Desktop/Server' /home/admin/デスクトップ/IoT/Tem
p
-su: mmount: コマンドが見つかりません
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# mount -t cifs -o vers=2.0, domain='AD', user='00220401626', password='Mm3001645$' 'C:/Users/00220401626/Desktop/Server' /home/admin/デスクトップ/IoT/Temp

Usage:
 mount [-lhV]
 mount -a [options]
 mount [options] [--source] <source> | [--target] <directory>
 mount [options] <source> <directory>
 mount <operation> <mountpoint> [<target>]

Mount a filesystem.

Options:
 -a, --all               mount all filesystems mentioned in fstab
 -c, --no-canonicalize   don't canonicalize paths
 -f, --fake              dry run; skip the mount(2) syscall
 -F, --fork              fork off for each device (use with -a)
 -T, --fstab <path>      alternative file to /etc/fstab
 -i, --internal-only     don't call the mount.<type> helpers
 -l, --show-labels       show also filesystem labels
 -n, --no-mtab           don't write to /etc/mtab
 -o, --options <list>    comma-separated list of mount options
 -O, --test-opts <list>  limit the set of filesystems (use with -a)
 -r, --read-only         mount the filesystem read-only (same as -o ro)
 -t, --types <list>      limit the set of filesystem types
     --source <src>      explicitly specifies source (path, label, uuid)
     --target <target>   explicitly specifies mountpoint
 -v, --verbose           say what is being done
 -w, --rw, --read-write  mount the filesystem read-write (default)

 -h, --help     display this help and exit
 -V, --version  output version information and exit

Source:
 -L, --label <label>     synonym for LABEL=<label>
 -U, --uuid <uuid>       synonym for UUID=<uuid>
 LABEL=<label>           specifies device by filesystem label
 UUID=<uuid>             specifies device by filesystem UUID
 PARTLABEL=<label>       specifies device by partition label
 PARTUUID=<uuid>         specifies device by partition UUID
 <device>                specifies device by path
 <directory>             mountpoint for bind mounts (see --bind/rbind)
 <file>                  regular file for loopdev setup

Operations:
 -B, --bind              mount a subtree somewhere else (same as -o bind)
 -M, --move              move a subtree to some other place
 -R, --rbind             mount a subtree and all submounts somewhere else
 --make-shared           mark a subtree as shared
 --make-slave            mark a subtree as slave
 --make-private          mark a subtree as private
 --make-unbindable       mark a subtree as unbindable
 --make-rshared          recursively mark a whole subtree as shared
 --make-rslave           recursively mark a whole subtree as slave
 --make-rprivate         recursively mark a whole subtree as private
 --make-runbindable      recursively mark a whole subtree as unbindable

For more details see mount(8).
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# mount -t cifs -o vers=2.0, domain='AD', user='00220401626', password='Mm3001645$' '//10.37.152.184/Server' /home/admin/デスクトップ/IoT/Temp

Usage:
 mount [-lhV]
 mount -a [options]
 mount [options] [--source] <source> | [--target] <directory>
 mount [options] <source> <directory>
 mount <operation> <mountpoint> [<target>]

Mount a filesystem.

Options:
 -a, --all               mount all filesystems mentioned in fstab
 -c, --no-canonicalize   don't canonicalize paths
 -f, --fake              dry run; skip the mount(2) syscall
 -F, --fork              fork off for each device (use with -a)
 -T, --fstab <path>      alternative file to /etc/fstab
 -i, --internal-only     don't call the mount.<type> helpers
 -l, --show-labels       show also filesystem labels
 -n, --no-mtab           don't write to /etc/mtab
 -o, --options <list>    comma-separated list of mount options
 -O, --test-opts <list>  limit the set of filesystems (use with -a)
 -r, --read-only         mount the filesystem read-only (same as -o ro)
 -t, --types <list>      limit the set of filesystem types
     --source <src>      explicitly specifies source (path, label, uuid)
     --target <target>   explicitly specifies mountpoint
 -v, --verbose           say what is being done
 -w, --rw, --read-write  mount the filesystem read-write (default)

 -h, --help     display this help and exit
 -V, --version  output version information and exit

Source:
 -L, --label <label>     synonym for LABEL=<label>
 -U, --uuid <uuid>       synonym for UUID=<uuid>
 LABEL=<label>           specifies device by filesystem label
 UUID=<uuid>             specifies device by filesystem UUID
 PARTLABEL=<label>       specifies device by partition label
 PARTUUID=<uuid>         specifies device by partition UUID
 <device>                specifies device by path
 <directory>             mountpoint for bind mounts (see --bind/rbind)
 <file>                  regular file for loopdev setup

Operations:
 -B, --bind              mount a subtree somewhere else (same as -o bind)
 -M, --move              move a subtree to some other place
 -R, --rbind             mount a subtree and all submounts somewhere else
 --make-shared           mark a subtree as shared
 --make-slave            mark a subtree as slave
 --make-private          mark a subtree as private
 --make-unbindable       mark a subtree as unbindable
 --make-rshared          recursively mark a whole subtree as shared
 --make-rslave           recursively mark a whole subtree as slave
 --make-rprivate         recursively mark a whole subtree as private
 --make-runbindable      recursively mark a whole subtree as unbindable

For more details see mount(8).
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# mount -t cifs -o vers=2.0, domain='AD', user='00220401626', password='Mm3001645$' //10.37.152.184/Server /home/admin/デスクトップ/IoT/Temp

Usage:
 mount [-lhV]
 mount -a [options]
 mount [options] [--source] <source> | [--target] <directory>
 mount [options] <source> <directory>
 mount <operation> <mountpoint> [<target>]

Mount a filesystem.

Options:
 -a, --all               mount all filesystems mentioned in fstab
 -c, --no-canonicalize   don't canonicalize paths
 -f, --fake              dry run; skip the mount(2) syscall
 -F, --fork              fork off for each device (use with -a)
 -T, --fstab <path>      alternative file to /etc/fstab
 -i, --internal-only     don't call the mount.<type> helpers
 -l, --show-labels       show also filesystem labels
 -n, --no-mtab           don't write to /etc/mtab
 -o, --options <list>    comma-separated list of mount options
 -O, --test-opts <list>  limit the set of filesystems (use with -a)
 -r, --read-only         mount the filesystem read-only (same as -o ro)
 -t, --types <list>      limit the set of filesystem types
     --source <src>      explicitly specifies source (path, label, uuid)
     --target <target>   explicitly specifies mountpoint
 -v, --verbose           say what is being done
 -w, --rw, --read-write  mount the filesystem read-write (default)

 -h, --help     display this help and exit
 -V, --version  output version information and exit

Source:
 -L, --label <label>     synonym for LABEL=<label>
 -U, --uuid <uuid>       synonym for UUID=<uuid>
 LABEL=<label>           specifies device by filesystem label
 UUID=<uuid>             specifies device by filesystem UUID
 PARTLABEL=<label>       specifies device by partition label
 PARTUUID=<uuid>         specifies device by partition UUID
 <device>                specifies device by path
 <directory>             mountpoint for bind mounts (see --bind/rbind)
 <file>                  regular file for loopdev setup

Operations:
 -B, --bind              mount a subtree somewhere else (same as -o bind)
 -M, --move              move a subtree to some other place
 -R, --rbind             mount a subtree and all submounts somewhere else
 --make-shared           mark a subtree as shared
 --make-slave            mark a subtree as slave
 --make-private          mark a subtree as private
 --make-unbindable       mark a subtree as unbindable
 --make-rshared          recursively mark a whole subtree as shared
 --make-rslave           recursively mark a whole subtree as slave
 --make-rprivate         recursively mark a whole subtree as private
 --make-runbindable      recursively mark a whole subtree as unbindable

For more details see mount(8).
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# mount -t cifs -o vers=2.0, domain='AD', user='00220401626', password='Mm3001645$' //10.37.152.184/Server /home/admin/デスクトップ/IoT/Temp

Usage:
 mount [-lhV]
 mount -a [options]
 mount [options] [--source] <source> | [--target] <directory>
 mount [options] <source> <directory>
 mount <operation> <mountpoint> [<target>]

Mount a filesystem.

Options:
 -a, --all               mount all filesystems mentioned in fstab
 -c, --no-canonicalize   don't canonicalize paths
 -f, --fake              dry run; skip the mount(2) syscall
 -F, --fork              fork off for each device (use with -a)
 -T, --fstab <path>      alternative file to /etc/fstab
 -i, --internal-only     don't call the mount.<type> helpers
 -l, --show-labels       show also filesystem labels
 -n, --no-mtab           don't write to /etc/mtab
 -o, --options <list>    comma-separated list of mount options
 -O, --test-opts <list>  limit the set of filesystems (use with -a)
 -r, --read-only         mount the filesystem read-only (same as -o ro)
 -t, --types <list>      limit the set of filesystem types
     --source <src>      explicitly specifies source (path, label, uuid)
     --target <target>   explicitly specifies mountpoint
 -v, --verbose           say what is being done
 -w, --rw, --read-write  mount the filesystem read-write (default)

 -h, --help     display this help and exit
 -V, --version  output version information and exit

Source:
 -L, --label <label>     synonym for LABEL=<label>
 -U, --uuid <uuid>       synonym for UUID=<uuid>
 LABEL=<label>           specifies device by filesystem label
 UUID=<uuid>             specifies device by filesystem UUID
 PARTLABEL=<label>       specifies device by partition label
 PARTUUID=<uuid>         specifies device by partition UUID
 <device>                specifies device by path
 <directory>             mountpoint for bind mounts (see --bind/rbind)
 <file>                  regular file for loopdev setup

Operations:
 -B, --bind              mount a subtree somewhere else (same as -o bind)
 -M, --move              move a subtree to some other place
 -R, --rbind             mount a subtree and all submounts somewhere else
 --make-shared           mark a subtree as shared
 --make-slave            mark a subtree as slave
 --make-private          mark a subtree as private
 --make-unbindable       mark a subtree as unbindable
 --make-rshared          recursively mark a whole subtree as shared
 --make-rslave           recursively mark a whole subtree as slave
 --make-rprivate         recursively mark a whole subtree as private
 --make-runbindable      recursively mark a whole subtree as unbindable

For more details see mount(8).
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# mount -t cifs -o vers=2.0,domain='AD',user='00220401626',password='Mm3001645$' //10.37.110.37.152.18452.184/Server /
home/admin/デスクトップ/IoT/Temp
mount error: could not resolve address for 10.37.110.37.152.18452.184: Unknown error
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# mount -t cifs -o vers=2.0,domain='AD',user='00220401626',password='Mm3001645$' //10.37.110.37.152.184/Server /home/a
dmin/デスクトップ/IoT/Temp
mount error: could not resolve address for 10.37.110.37.152.184: Unknown error
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# mount -t cifs -o vers=2.0,domain='AD',user='00220401626',password='Mm3001645$' //10.37.152.184/Server /home/admin/デ
スクトップ/IoT/Temp
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Code# cd ../Temp/
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Temp# ls -al
合計 16
drwxr-xr-x 2 root  root     0  5月 15 16:14 .
drwxr-xr-x 5 admin admin 4096  5月 15 15:06 ..
-rwxr-xr-x 1 root  root  8571  5月 15 15:03 test_5.csv
root@InterfaceLinuxSystem:/home/admin/デスクトップ/IoT/Temp# ls -al
合計 16
drwxr-xr-x 2 root  root     0  5月 15 16:50 .
drwxr-xr-x 5 admin admin 4096  5月 15 15:06 ..
-rwxr-xr-x 1 root  root     0  5月 15 16:50 test.txt
-rwxr-xr-x 1 root  root  8571  5月 15 15:03 test_5.csv
